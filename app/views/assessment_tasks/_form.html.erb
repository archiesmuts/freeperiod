<%= form_with(model: assessment_task, local: true) do |form| %>
  <% if assessment_task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(assessment_task.errors.count, "error") %> prohibited this assessment_task from being saved:</h2>

      <ul>
      <% assessment_task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, id: :assessment_task_name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.collection_select :assessment_type_id, AssessmentType.order(:name), :id, :name, { include_blank: true  }, {class: "form-control"} %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
