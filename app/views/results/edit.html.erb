<div class="row mt-3">
  <div class="col-xl-6 offset-xl-3">
    <h1>Editing Individual Result</h1>
    <div class="card text-white bg-dark mb-3">
      <div class="card-header"><%= @result.assessment.name.truncate(60) %></div>
      <div class="card-body">
        <%= render 'edit_form', result: @result %>
      </div>
    </div>
  </div>
</div>
