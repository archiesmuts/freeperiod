$(".delete-part").bind('ajax:success', function() {
  $(this).parents('div.part-card').fadeOut(500, function(){
  	$(this).remove();
    $('#list-parts').sizeParts();
    $('#totalValue').getTotal();
});
