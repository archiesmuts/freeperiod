// Add the dialog title
$('#part-dialog h4').html("Edit assessment part details");

// Render the new form
$('.modal-body').html('<%= j render "parts/edit_form" %>');

// Show the dynamic dialog
$('#part-dialog').modal("show");

// Set focus to the first element
$('#part-dialog').on('shown.bs.modal', function () {
  $('#part_name').focus();
});
