// Add the dialog title
$('#event-dialog h4').html("New Event");

// Render the new form
$('.modal-body').html('<%= j render "events/new_remote_form" %>');

// Show the dynamic dialog
$('#event-dialog').modal("show");
