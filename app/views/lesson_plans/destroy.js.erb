$(".delete-lesson-plan").bind('ajax:success', function() {
  $(this).parents('div.lesson-plan-card').fadeOut(500, function(){
  	$(this).remove();
  });
  $('#lesson_plan_registration_klass_id').focus();
});
