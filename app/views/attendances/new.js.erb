// Add the dialog title
$('#attendance-dialog h4').html("Record attendance");

// Render the new form
$('.modal-body').html('<%= j render "attendances/new_form", attendable: @user_registration  %>');

// Show the dynamic dialog
$('#attendance-dialog').modal("show");

// Set focus to the first element
$('#attendance-dialog').on('shown.bs.modal', function () {
  $('#attendance_student_score').focus();
});
