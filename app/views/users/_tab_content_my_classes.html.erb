<div class="jumbotron">
  <h1>My Classes</h1>
  <p class="lead">
    Registration and Subject Classes
  </p>
</div>

<div class="d-flex flex-wrap" id="list-user-registrations">
  <% if current_user.user_registrations.any? %>
    <%= render current_user.user_registrations %>
  <% else %>
    <%= render "nothing_to_list" %>
  <% end %>
</div> <!-- .card-deck -->
