<div class="jumbotron">
  <h1>Celebrate my achievements. <small><em>Big and small</em></small> </h1>
  <p>
    Remember to celebrate both the big and the little achievements.
    You know when have that <em>"Yes. I did it!"</em> moments? List all of those here.
  </p>
    <%= link_to new_user_achievement_path(current_user), remote: true, class: "btn btn-primary btn-block-xs" do %>
      <%= fa_icon("plus", text: "Add an Achievement") %>
    <% end %>
</div>

<div class="d-flex flex-wrap" id="list-achievements">
    <% if current_user.achievements.any? %>
      <%= render "achievements/achievements", achievable: @user %>
    <% else %>
      <li class="list-group-item list-group-item-info no-achievements-to-list">
        <h4 class="list-group-item-heading">No Achievements to list yet.</h4>
        Remember to also celebrate the little things. You know when have that <em>"Yes. I did it!"</em> moments?
      </li>
    <% end %>
</div> <!-- .card-deck -->
