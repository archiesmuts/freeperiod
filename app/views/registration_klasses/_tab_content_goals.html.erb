<div class="jumbotron">
  <h2>Our Class Goals and Action Plans</h2>
  <p class="h5"><span class="badge badge-dark"><%= @registration_klass.name %></span></p>
  <p>This is a template for a simple marketing or informational website. It includes a large callout called a jumbotron and three supporting pieces of content. Use it as a starting point to create something more unique.</p>
    <%= link_to new_registration_klass_goal_path(@registration_klass), remote: true,
        class: "btn btn-primary btn-block-xs float-right" do %>
      <%= fa_icon("plus", text: "Add a Class Goal") %>
    <% end %>

</div>

<div class="d-flex flex-wrap" id="list-goals">
    <% if @registration_klass.goals.any? %>
      <%= render "goals/goals", goalable: @registration_klass %>
    <% else %>
      <li class="list-group-item list-group-item-info no-goals-to-list">
        No Goals Recorded yet
      </li>
    <% end %>
</div>
