// Add the dialog title
$('#achievement-dialog h4').html("Add an Achievement ");

// Render the new form
$('.modal-body').html('<%= j render "achievements/new_form" %>');

// Show the dynamic dialog
$('#achievement-dialog').modal("show");

// Set focus to the first element
$('#achievement-dialog').on('shown.bs.modal', function () {
  $('#achievement_title').focus();
});
