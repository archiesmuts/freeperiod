<div class="jumbotron">
  <h1> Registration and Subject Classes</h1>
  <p>This is a template for a simple marketing or informational website. It includes a large callout called a jumbotron and three supporting pieces of content. Use it as a starting point to create something more unique.</p>
  <%= link_to school_registration_klasses_path(@school), class: "btn btn-primary btn-block-xs" do %>
    <%= fa_icon("list-alt", text: "Manage classes") %>
  <% end %>
</div>

<div class="d-flex flex-wrap" id="list-registration-klasses">
    <% if @school.registration_klasses.any? %>
    <table class="table">
      <thead>
        <tr>
          <th>Class Name</th>
          <th>Class Type</th>
          <th>Course</th>
          <th>Year</th>
          <th colspan="3"></th>
        </tr>
      </thead>
      <tbody id="list-registration-klasses">
        <%= render @school.registration_klasses %>
      </tbody>
    </table>
    <% else %>
      <li class="list-group-item list-group-item-info no-registration-klasses-to-list">
        Nothing to list
      </li>
    <% end %>
</div> <!-- .card-deck -->
