$(".delete-goal").bind('ajax:success', function() {
  $(this).parents('div.goal-card').fadeOut(500, function(){
  	$(this).remove();
    $('#list-goals').sizeGoals();
    $('#sizeUserGoals').sizeGoals();
  });
});
