<% commentable.comments.each do |comment| %>
  <li class="list-group-item">
    <small>
      <%= fa_icon "comment" %>&nbsp;<em><%= comment.user.name %>&nbsp;
        ...<%= time_ago_in_words(comment.created_at) %> ago.</em>
    </small>
  </li>
<% end %>
